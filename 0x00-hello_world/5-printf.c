#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A program that writes strings to stdout using printf function
 *
 * Return: always 0 (success)
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
